rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // 로그인한 사용자는 모든 것 허용
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}